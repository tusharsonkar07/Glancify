# ── Set this in your Vercel project dashboard under Settings → Environment Variables ──
# This points the frontend to your Railway backend URL.
# The vite.config.js proxy handles /api during local development automatically.

VITE_API_URL=https://glancify-production.up.railway.app/api
